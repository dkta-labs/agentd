name: Canonical naming

on:
  push:
  pull_request:

permissions:
  contents: read

jobs:
  naming:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v4
      - name: Reject retired project names and workstation paths
        run: scripts/check-naming.sh
